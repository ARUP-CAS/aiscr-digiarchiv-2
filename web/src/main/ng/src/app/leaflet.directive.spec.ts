import { LeafletDirective } from './leaflet.directive';

describe('LeafletDirective', () => {
  it('should create an instance', () => {
    const directive = new LeafletDirective();
    expect(directive).toBeTruthy();
  });
});
